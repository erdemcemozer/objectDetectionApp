import React from "react";
import {
  StyleSheet,
  Text,
  View,
  Image,
  TouchableOpacity,
  TouchableWithoutFeedback,
  Alert,
  SafeAreaView,
  Button,
  SliderComponent,
} from "react-native";

function PictureSendingScreen(props) {
  return (
    <SafeAreaView>
      <text>SELAM</text>
    </SafeAreaView>
  );
}

export default PictureSendingScreen;
